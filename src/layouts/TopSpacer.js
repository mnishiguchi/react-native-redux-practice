import React from 'react';
import { View } from 'react-native';

const TopSpacer = () => (
  <View
    style={{
      height: 24,
      backgroundColor: '#333',
    }}
  />
);

export default TopSpacer;
